services:
  - type: web
    name: clinic-bs-khang
    env: node
    buildCommand: npm install && npx vite build
    startCommand: node start.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000